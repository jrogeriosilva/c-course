#include <stdio.h>
#include <math.h>


int main (){
    int a, b, c;
    float d;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    
  d= c - ((3*a) + (2*b))
  / (c - ( c*pow((a+b),2)/ sqrt(b) ));


    printf ("%f",d);
    return 0;
}
